{
  "verdict": "REVERT",
  "reasoning": "iter-042: tile_assign no-exp no-div via per-Gaussian precomputed m2_thresh + opacity_above_floor sentinel array. Theory: reformulating contrib >= contrib_floor as num <= det * m2_thresh removes 1 div and 1 exp per pair (P~294k) at the cost of 1 log per Gaussian (M~234k, parallel). Per-pair cycle budget drops from ~50 to ~20. Result: REGRESSION. tile_assign p50 1.67 -> 1.91 ms; sum 50.9 -> 57.9 ms. PSNR bit-identical (correctness preserved). Likely cause: the side arrays (m2_thresh_g + opacity_above_floor = 1.2 MB) plus the random opacity_above_floor[g] branch in the per-pair stripe cost more than the inline div + exp. The original code's div + exp were already pipelined behind the (random) covs_2d load. The new code's extra index lookup creates a serial dependency chain through the byte sentinel. Reverting; iter-028 hit the same pattern with a different side-array shape. Future: if we ever need this, merge the threshold into project's per-Gaussian sweep (no new pool.wait) or fold opacity_above_floor into m2_thresh_g via a -inf sentinel."
}
