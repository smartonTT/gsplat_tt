// Placeholder so the static library has at least one translation unit.
// iter-001 replaces this with thread_pool + scaffolding.
namespace gsplat_cpu {
[[maybe_unused]] int placeholder() { return 0; }
}
